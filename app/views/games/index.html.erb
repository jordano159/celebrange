<p style="color: green"><%= notice %></p>

<h1 class="mx-auto">משחקים</h1>

<div id="games">
  <% @games.each do |game| %>
    <%= render game %>
    <p>
      <%= link_to game.name, game %>
    </p>
  <% end %>
</div>

<%= link_to "משחק חדש", new_game_path %>


