<%= form_with(model: celeb) do |form| %>
  <% if celeb.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(celeb.errors.count, "error") %> prohibited this celeb from being saved:</h2>

      <ul>
        <% celeb.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :game_id, style: "display: block" %>
    <%= form.text_field :game_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
